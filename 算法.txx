算法 

栈、队列：
切片
添加、删除使用 append()
返回使用切片 *Stack=(*Stack)[:index] queue[1:]

是否传入指针？
栈传入指针，修改了切片底层数组的容量

队列不传入指针，出队只改变了切片起始位置，没有影响底层数组

append() 会动态影响容量


数组：
双指针
滑动窗口
哈希表（表存储出现频率）
前缀和，计算前缀和数组，相减得到区间和
模拟，打印特定规律图形（循环不变量，统一前开后闭）

链表：
单链表
// 单链表：
// 构造链表时创建虚拟头节点
// 添加节点时创建新节点
// 遍历、查询时创建新指针
// 遇到 index 时判断是否存在

反转链表

